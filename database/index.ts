export { default as Event } from './event.model';
export { default as Booking } from './booking.model';

// Barrel file to import models from a single entrypoint:
// import { Event, Booking } from 'database';
